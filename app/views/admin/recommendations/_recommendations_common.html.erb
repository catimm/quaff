<div class="row no-margin first-row">
	<div class="container first-row-container">
		<div class="col-xs-12">
			<h1 class="section-header">Recos</h1>
		</div>
		<div class="col-xs-12 no-padding">
			<div class="row no-margin">
				<div class="col-xs-6 col-sm-3 no-padding-right">
					<%= link_to "Customers", admin_recommendations_path, class: "drink-preference-view-option #{if current_section == "Customers" then "current" else "" end}" %>
				</div>
				<div class="col-xs-6 col-sm-3">
					<%= link_to "Free Curations", free_curations_path, class: "drink-preference-view-option #{if current_section == "Free" then "current" else "" end}" %>
				</div>
			</div>
		</div>
	</div>
</div>