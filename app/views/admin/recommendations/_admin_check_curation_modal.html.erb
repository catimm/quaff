<div class="modal-header">
	<div class="row no-margin">
		<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
		<div class="col-sm-8">
			<h3>Drinks planned for <span class="current-selected-user" id="current-user-recommendations"><%= @account_owner[0].username %></span></h3>
		</div>
		<div class="col-sm-4" id="<%= @free_curation.id %>">
			<h4 class="ready-to-share-text">Ready to share with customer?</h4>
			<% if @free_curation.share_admin_prep == true %>
				<%= check_box_tag :share_curation_with_customer, 'yes', true %>
			<% else %>
				<%= check_box_tag :share_curation_with_customer, 'yes', false %>
			<% end %>
		</div>
	</div>
</div>
<div class="modal-body"></div>