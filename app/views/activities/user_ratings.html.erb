<%= render 'layouts/header' %> 
	<div class="row no-margin first-row">
		<div class="container first-row-container">
			<div class="col-sm-5 no-padding">
				<h1 class="section-header">Recent Activity</h1>
			</div>
			<div class="col-sm-7">
				<div class="row no-margin">
					<div class="col-sm-2"></div>
					<div class="col-sm-3 text-center">
						<%= link_to "My ratings", recent_user_ratings_path(current_user.id), class: "drink-preference-view-option chosen" %>
					</div>
					<div class="col-sm-4 text-center">
						<%= link_to "Friends ratings", recent_friend_ratings_path(current_user.id), class: "drink-preference-view-option" %>
					</div>
				</div>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="container">
			<%= render 'recent_user_ratings' %>
		</div>
	</div>
<%= render 'layouts/footer' %>