<%= render 'layouts/header' %> 
	<div class="row no-margin first-row">
		<div class="container first-row-container">
			<div class="col-sm-5 no-padding">
				<h1 class="section-header">Drink Supply</h1>
			</div>
			<div class="col-sm-1"></div>
			<div class="col-sm-1">
				<%= link_to "Cooler", user_supply_path('cooler'), class: "drink-preference-view-option #{@cooler_chosen}", id: "cooler" %>
			</div>
			<div class="col-sm-2">
				<%= link_to "Cellar", user_supply_path('cellar'), class: "drink-preference-view-option #{@cellar_chosen}", id: "cellar" %>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="container" id="preference-view-choice">
			<% if @view == "cooler" %>
				<%= render 'user_cooler' %>
			<% else %>
				<%= render 'user_cellar' %>
			<% end %>
		</div>
	</div>
	<div id="remove_drink_confirmation" class="modal">
		<div class="modal-header">
			<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
			<h3>Remove drink confirmation</h3>
		</div>
		<div class="modal-body">
			<div class="row no-margin">
				<div class="col-sm-12">
					<h4>Remove this drink from your supply without rating it?</h4>
				</div>
			</div>
				<div class="col-sm-2"></div>
				<div class="col-sm-8 remove-button-holder">
					<%= link_to "#", :class => 'subtract-drink-confirmation text-center btn btn-danger remove-drink-button' do %>
				<i class="fa fa-minus"></i>
				Remove drink
			<% end %>
				</div>
		</div>
</div>
<%= render 'layouts/footer' %>