$(function() {
	console.log("gets here");
	// show estimate impact
	$("#drinks-frequency-meaning").html("<%= escape_javascript(render 'layouts/per_delivery_estimate' ) %>");
	// highlight number chosen
	$("#<%= @delivery_frequency%>).addClass("chosen");
	$("#<%= @delivery_frequency%>).siblings().removeClass("chosen")
});